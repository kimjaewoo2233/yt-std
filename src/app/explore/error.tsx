'use client';
import React from 'react';
import ErrorMessage from '@/components/ErrorMessage';

/**
 * error.tsx는 use client 가 필수임
 */
const error = () => {
  return <ErrorMessage />;
};

export default error;
